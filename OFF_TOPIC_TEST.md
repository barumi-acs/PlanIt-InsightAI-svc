# Off-topic 방어 로직 테스트 가이드

## 🎯 목적
PlanIt 서비스 도메인과 무관한 질문을 거절하는 기능 테스트

## ✅ 정상 질문 (On-topic) - 답변해야 함

### 1. 할 일 완료 관련
```json
{
  "user_id": "test-user-001",
  "query": "지난 주에 할 일을 가장 많이 완료한 요일은?"
}
```
**예상 답변**: 데이터 조회 후 구체적인 요일과 횟수 제공

### 2. 미룸 패턴 분석
```json
{
  "user_id": "test-user-001",
  "query": "어느 요일에 할 일을 가장 많이 미루나요?"
}
```
**예상 답변**: 미룸이 많은 요일 분석 및 개선 조언

### 3. 완료율 조회
```json
{
  "user_id": "test-user-001",
  "query": "이번 달 완료율은 얼마나 되나요?"
}
```
**예상 답변**: 완료율 계산 및 격려 메시지

### 4. 생산성 패턴
```json
{
  "user_id": "test-user-001",
  "query": "요일별 생산성이 어떻게 되나요?"
}
```
**예상 답변**: 요일별 완료율 분석

### 5. 최근 활동
```json
{
  "user_id": "test-user-001",
  "query": "최근에 완료한 할 일 보여주세요"
}
```
**예상 답변**: 최근 완료한 할 일 목록

---

## ❌ Off-topic 질문 - 거절해야 함

### 1. 날씨 질문
```json
{
  "user_id": "test-user-001",
  "query": "오늘 날씨 어때?"
}
```
**예상 답변**:
```
저는 할 일 관리와 생산성 향상을 돕는 PlanIt AI 코치입니다. 
사용자의 목표 달성과 관련된 질문을 남겨주시면 최선을 다해 답변해 드릴게요! 😊

예를 들어 이런 질문을 해보세요:
• 지난 주에 가장 생산적이었던 요일은?
• 이번 달 완료율은 얼마나 되나요?
• 어느 요일에 할 일을 가장 많이 미루나요?
• 최근 완료한 할 일을 보여주세요.
```

### 2. 코딩 질문
```json
{
  "user_id": "test-user-001",
  "query": "Python으로 리스트를 정렬하는 방법 알려줘"
}
```
**예상 답변**: 위와 동일한 거절 메시지

### 3. 일반 상식
```json
{
  "user_id": "test-user-001",
  "query": "한국의 수도는 어디야?"
}
```
**예상 답변**: 위와 동일한 거절 메시지

### 4. 수학 문제
```json
{
  "user_id": "test-user-001",
  "query": "2+2는 얼마야?"
}
```
**예상 답변**: 위와 동일한 거절 메시지

### 5. 번역 요청
```json
{
  "user_id": "test-user-001",
  "query": "Hello를 한국어로 번역해줘"
}
```
**예상 답변**: 위와 동일한 거절 메시지

### 6. 요리 질문
```json
{
  "user_id": "test-user-001",
  "query": "김치찌개 만드는 법 알려줘"
}
```
**예상 답변**: 위와 동일한 거절 메시지

### 7. 뉴스/시사
```json
{
  "user_id": "test-user-001",
  "query": "최근 뉴스 알려줘"
}
```
**예상 답변**: 위와 동일한 거절 메시지

---

## 🧪 테스트 방법

### 1. Swagger UI 사용
1. http://localhost:8085/docs 열기
2. POST /ai/chat/query 선택
3. "Try it out" 클릭
4. 위의 질문 중 하나 입력
5. "Execute" 클릭
6. 응답 확인

### 2. PowerShell 사용
```powershell
# On-topic 질문 (정상 답변)
$body = @{
    user_id = "test-user-001"
    query = "지난 주에 할 일을 가장 많이 완료한 요일은?"
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:8085/ai/chat/query" -Method Post -Body $body -ContentType "application/json"

# Off-topic 질문 (거절 메시지)
$body = @{
    user_id = "test-user-001"
    query = "오늘 날씨 어때?"
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:8085/ai/chat/query" -Method Post -Body $body -ContentType "application/json"
```

---

## 📊 테스트 체크리스트

### On-topic 질문 (5개)
- [ ] 할 일 완료 관련 질문 → 데이터 조회 및 분석 답변
- [ ] 미룸 패턴 질문 → 패턴 분석 및 조언
- [ ] 완료율 질문 → 완료율 계산 및 격려
- [ ] 생산성 패턴 질문 → 요일별 분석
- [ ] 최근 활동 질문 → 목록 제공

### Off-topic 질문 (7개)
- [ ] 날씨 질문 → 거절 메시지
- [ ] 코딩 질문 → 거절 메시지
- [ ] 일반 상식 → 거절 메시지
- [ ] 수학 문제 → 거절 메시지
- [ ] 번역 요청 → 거절 메시지
- [ ] 요리 질문 → 거절 메시지
- [ ] 뉴스/시사 → 거절 메시지

---

## 💡 예상 동작

### On-topic 질문 처리 흐름
```
사용자 질문 → 시스템 프롬프트 확인 → 도메인 내 질문 판단
→ Tool 실행 → 데이터 조회 → 분석 → 친근한 답변 생성
```

### Off-topic 질문 처리 흐름
```
사용자 질문 → 시스템 프롬프트 확인 → 도메인 외 질문 판단
→ Tool 실행 없음 → 정중한 거절 메시지 즉시 반환
```

---

## 🔒 보안 및 비즈니스 가치

### 보안
- 사전 학습된 지식 유출 방지
- 서비스 범위 외 책임 회피
- 리소스 낭비 방지 (불필요한 Tool 실행 차단)

### 비즈니스
- 명확한 서비스 정체성 유지
- 사용자 기대치 관리
- 브랜드 일관성 확보
- 고객 만족도 향상 (명확한 역할 인지)

---

## 📝 개선 제안

### 추가 가능한 기능
1. **카테고리별 거절 메시지**
   - 날씨: "날씨 정보는 제공하지 않지만, 날씨에 따른 할 일 계획은 도와드릴 수 있어요!"
   - 코딩: "코딩 질문은 답변하지 않지만, 코딩 학습 목표 관리는 도와드릴 수 있어요!"

2. **질문 유도**
   - Off-topic 질문 후 관련된 On-topic 질문 제안
   - 예: "대신 '이번 주 학습 목표 달성률은?'과 같은 질문은 어떠세요?"

3. **로깅 및 분석**
   - Off-topic 질문 빈도 추적
   - 자주 나오는 Off-topic 패턴 분석
   - 시스템 프롬프트 개선에 활용
